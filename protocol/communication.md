# Test bus placeholder

This is NOT the protocol. The canonical protocol is `protocol/communication.md` in `terchris/urb-agents`, read remotely by every agent; it is never copied. This file exists only as the read-probe target for `verify-token.sh` on the test bus (PLAN-V006 phase 2.5).
